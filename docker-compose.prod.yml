version: '3.8'

services:
  tladmin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tladmin-prod
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - SITE_ACCESS_PASSWORD=${SITE_ACCESS_PASSWORD}
    networks:
      - tladmin-network
    depends_on:
      - postgres

  postgres:
    image: postgres:16-alpine
    container_name: tladmin-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=appuser
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=tesland
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - tladmin-network
    ports:
      - "5432:5432"

  nginx:
    image: nginx:alpine
    container_name: tladmin-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - tladmin
    networks:
      - tladmin-network

volumes:
  postgres_data:

networks:
  tladmin-network:
    driver: bridge
