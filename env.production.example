# TLadmin Production Environment Variables
# ==========================================
# Kopieer dit bestand naar .env.production op de Hetzner VPS
# en vul de juiste waarden in

# Database (lokale PostgreSQL container)
DATABASE_URL=postgresql://appuser:CHANGE_ME_STRONG_PASSWORD@postgres:5432/tesland?schema=public

# JWT Secret (genereer een sterke random string)
# Genereer met: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET_AT_LEAST_32_CHARS

# Optioneel: site-wachtwoord voor app.tesland.com (één wachtwoord voor alle bezoekers)
# Als gezet: bezoekers moeten eerst dit wachtwoord invoeren op /_site-access
# SITE_ACCESS_PASSWORD=jouw-geheim-wachtwoord

# PostgreSQL Password (moet overeenkomen met DATABASE_URL)
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Production settings
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# ==============================================================
# BELANGRIJK: Voordat je de app start:
# ==============================================================
# 1. Verander POSTGRES_PASSWORD naar een sterk wachtwoord
# 2. Update het wachtwoord ook in DATABASE_URL
# 3. Genereer een sterke JWT_SECRET:
#    openssl rand -base64 32
# ==============================================================
